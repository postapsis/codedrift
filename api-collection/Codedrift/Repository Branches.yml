info:
  name: Repository Branches
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/repository/:repositoryId/branches"
  params:
    - name: repositoryId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
