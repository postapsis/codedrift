info:
  name: List Repositories
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/repositories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
