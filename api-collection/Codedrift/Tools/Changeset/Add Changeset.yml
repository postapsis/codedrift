info:
  name: Add Changeset
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/tools/changeset/add"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Auth refactor",
        "description": "Changes related to auth",
        "filesPaths": ["flightdrift-frontend/package.json"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
