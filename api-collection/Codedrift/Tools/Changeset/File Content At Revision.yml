info:
  name: File Content At Revision
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/tools/changeset/file-content-at-revision"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "filePath": "flightdrift-frontend/src/routes/auth/signin.tsx",
        "revision": "87369c2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
