info:
  name: Changeset Files
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/tools/changeset/files"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
