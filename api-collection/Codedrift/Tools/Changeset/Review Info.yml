info:
  name: Review Info
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/tools/changeset/review-info"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "reviewId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
