info:
  name: Repository Reviews
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/repository/:repositoryId/reviews"
  params:
    - name: repositoryId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
