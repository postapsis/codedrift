info:
  name: Review Changesets
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/review/:reviewId/changesets"
  params:
    - name: reviewId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
