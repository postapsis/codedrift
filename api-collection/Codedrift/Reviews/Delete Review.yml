info:
  name: Delete Review
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/review/:reviewId"
  params:
    - name: reviewId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
