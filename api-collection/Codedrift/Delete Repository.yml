info:
  name: Delete Repository
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/repository/:repositoryId"
  params:
    - name: repositoryId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
